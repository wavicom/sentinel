# specify path to wavi.conf or leave blank
# default is the same as WaviCore
#wavi_conf=/home/username/.wavicore/wavi.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
